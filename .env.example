# Copy file này thành .env.local rồi điền key thật vào (.env.local đã bị .gitignore, không bao giờ lên GitHub)
# Lấy key miễn phí tại: https://aistudio.google.com/apikey
GEMINI_API_KEY= YOUR_API_KEY_HERE
